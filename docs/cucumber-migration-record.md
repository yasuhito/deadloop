# Cucumber 移行記録

この記録は [分類表](cucumber-test-classification.md) の ID と受け入れ仕様の対応を追跡する。受け入れシナリオで完全に置換した Vitest だけを削除し、局所的な診断価値がある Vitest は残す。

## Issue #128: 公開設定と安全な既定値

受け入れ仕様の正本は [`acceptance/features/public-configuration.feature.md`](../acceptance/features/public-configuration.feature.md) である。ステップ定義は、設定の解析値を直接 assertion せず、決定論的なテスト用アダプターを通して実際の状態表示、エージェント起動引数、CI 代替判断を観測する。

### 元から Vitest 継続の ID

次の 22 件は分類表の時点から Vitest 継続であり、今回再分類したものではない。

| 分類 ID | 継続理由 |
|---|---|
| T082 | 対応する cron 形式の解析を局所的に診断する純粋関数である。 |
| T083 | cron 文字列の空白除去を局所的に診断する純粋関数である。 |
| T084 | 未対応 cron の拒否を局所的に診断する純粋関数である。 |
| T085 | 0 分間隔の拒否を局所的に診断する純粋関数である。 |
| T086 | 猶予期間外のスロット計算を局所的に診断する純粋関数である。 |
| T087 | 取り逃したスロットの内部記録を局所的に診断する。 |
| T088 | 現在の cron スロット計算を局所的に診断する純粋関数である。 |
| T089 | 最終実行時刻からの次回スロット計算を局所的に診断する純粋関数である。 |
| T090 | 古い候補を除外するスロット計算を局所的に診断する純粋関数である。 |
| T091 | テンプレート文字列の置換を局所的に診断する。 |
| T092 | 自動化スクリプト用の環境変数組み立てを局所的に診断する。 |
| T102 | Worker 起動方針の静的な文言契約を局所的に診断する。 |
| T105 | Worker・Reviewer モデルからテンプレート値への変換を局所的に診断する。 |
| T106 | Worker 種別からテンプレート値への変換を局所的に診断する。 |
| T107 | Reviewer 種別からテンプレート値への変換を局所的に診断する。 |
| T112 | CI 代替コマンドの文字列整形を局所的に診断する。 |
| T113 | 自動マージ状態からテンプレート値への変換を局所的に診断する。 |
| T114 | 外部レビュー状態からテンプレート値への変換を局所的に診断する。 |
| T130 | ソース更新時刻から警告を作る純粋関数を局所的に診断する。 |
| T131 | 表示 ID の小文字 slug 化を局所的に診断する純粋関数である。 |
| T132 | 記号だけの表示 ID に対する代替値を局所的に診断する純粋関数である。 |
| T133 | 空の表示 ID に対する代替値を局所的に診断する純粋関数である。 |

### Cucumber 候補だった ID の処理

分類表で Cucumber 候補だった 36 件は、すべて次のいずれかへ対応付けた。既存 Vitest は、設定解析の失敗箇所を絞る低レベル診断として残す。

| 分類 ID | 処理 | 移行先または ID ごとの再分類理由 |
|---|---|---|
| T076 | 移行済み | 「環境変数で指定した設定ファイルを状態表示に使う」 |
| T077 | 移行済み | 「利用者設定を同梱設定より優先して状態表示に使う」 |
| T078 | 移行済み | 「利用者設定がない場合は同梱設定を状態表示に使う」 |
| T079 | 同じ保証へ統合 | 安全設定、エージェント、自動化の利用者向け既定動作を T080、T095、T099、T108、T110、T111 の移行先で観測する。 |
| T080 | 移行済み | 「設定を省略した場合は標準の自動化を表示する」 |
| T081 | 移行済み | 「空の自動化設定は自動化を無効のまま表示する」 |
| T093 | Vitest 継続へ再分類 | 指示ファイル名から指示文を作る文字列整形であり、起動プロンプト全体とは分離した方が失敗原因を特定できる。 |
| T094 | Vitest 継続へ再分類 | 明示指示文と指示ファイルの優先順位は文字列組み立て内部の契約であり、起動結果へ統合すると原因が不明瞭になる。 |
| T095 | 移行済み | 「設定を省略した Worker は pi で起動する」 |
| T096 | 同じ保証へ統合 | 「ローカルの Worker の種別とモデルは共有方針より優先する」で明示した pi の実起動を観測する。 |
| T097 | 同じ保証へ統合 | 「指定した Worker エージェントとモデルで起動する」で claude の実起動を観測する。 |
| T098 | Vitest 継続へ再分類 | 不正な Worker 種別の解析エラーを局所的に診断する契約であり、エージェントは起動されない。 |
| T099 | 移行済み | 「設定を省略した Reviewer は pi で起動する」 |
| T100 | 同じ保証へ統合 | 「指定した Reviewer エージェントとモデルで起動する」で claude の実起動を観測する。 |
| T101 | Vitest 継続へ再分類 | 不正な Reviewer 種別の解析エラーを局所的に診断する契約であり、エージェントは起動されない。 |
| T103 | 同じ保証へ統合 | 「指定した Worker エージェントとモデルで起動する」でモデル引数を観測する。 |
| T104 | 同じ保証へ統合 | 「指定した Reviewer エージェントとモデルで起動する」でモデル引数を観測する。 |
| T108 | 移行済み | 「自動マージは設定を省略すると無効と表示する」 |
| T109 | 移行済み | 「自動マージはローカルで明示した場合だけ有効と表示する」 |
| T110 | 移行済み | 「CI 代替は設定を省略するとインフラ障害でも実行しない」で実際の代替判断を観測する。mode などの低レベル既定値は Vitest に残す。 |
| T111 | 移行済み | 「外部レビューは設定を省略すると無効と表示する」 |
| T115 | Vitest 継続へ再分類 | `driverFile` が正規化で失われないことを診断する設定変換契約であり、ドライバー実行とは分離する。 |
| T116 | Vitest 継続へ再分類 | tick ごとの再読込順序を検査する低レベル統合であり、単一回の利用者操作として表現すると契機が曖昧になる。 |
| T117 | Vitest 継続へ再分類 | 共有方針が存在しない場合の後方互換解析経路を、取得失敗と切り分けて診断する。 |
| T118 | 同じ保証へ統合 | 「共有方針はローカルで省略した Worker の種別とモデルを補う」で共有モデルを実起動に観測する。 |
| T119 | Vitest 継続へ再分類 | 共有された指示ファイル名から指示文を作る文字列整形であり、T093 と同じ低レベル診断境界で守る。 |
| T120 | 同じ保証へ統合 | 「ローカルの Worker の種別とモデルは共有方針より優先する」でローカルモデルを実起動に観測する。 |
| T121 | Vitest 継続へ再分類 | リポジトリ自身の `deadloop.json` 全体を解析できるかを検査する静的な配布物境界である。 |
| T122 | 移行済み | 「共有方針の空の自動化設定は自動化を無効のまま表示する」 |
| T123 | 移行済み | 「共有方針はローカルで省略した自動化を表示へ反映する」 |
| T124 | Vitest 継続へ再分類 | 共有方針の `driverFile` を自動化へ結合する低レベル設定変換であり、ドライバー実行とは分離する。 |
| T125 | 移行済み | 「共有方針はローカルで省略した外部レビューを表示へ反映する」 |
| T126 | Vitest 継続へ再分類 | 共有方針の禁止キーを拒否する解析エラーを、方針取得や表示から切り分けて診断する。 |
| T127 | Vitest 継続へ再分類 | 方針取得器へ渡す基準ブランチ引数の低レベル呼出契約である。 |
| T128 | Vitest 継続へ再分類 | 壊れた JSON から内部の status reason を作る解析診断であり、正常設定の表示契約とは異なる。 |
| T129 | Vitest 継続へ再分類 | スケジューラーロック所有者と再読込後 ID の内部不変条件を診断する低レベル統合である。 |

### 意図的失敗と成功確認

2026-07-16 に「設定を省略した Worker は pi で起動する」の期待値だけを一時的に `intentionally-wrong` へ変更し、次を実行した。

```bash
npx cucumber-js --name '^設定を省略した Worker は pi で起動する$'
```

終了コードは 1 で、実際の起動コマンド先頭 `pi` と不正な期待値の差、シナリオ位置 `acceptance/features/public-configuration.feature.md:37`、ステップ定義位置が表示された。期待値を復元後、`npm run test:acceptance` は 19 scenarios / 79 steps で成功した。一時変更は残していない。

最終確認では `npm run check` を実行し、Cucumber、Vitest、lint、型検査、シェル構文検査、配布物検査が成功した。
