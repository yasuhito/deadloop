# deadloop

GitHub Issue から実装・PR 作成・レビュー・マージまでを Pi 上で自動で回すループエンジニアリングツール。このファイルはプロジェクトの用語集であり、実装の詳細は含めない。

## Language

**deadloop core**:
GitHub Issue / PR の状態遷移、安全ゲート、ループ上の判断を担う、実行環境に依存しない中核。定期実行、worktree / session 管理、エージェント CLI ごとの差異は含めない。
_Avoid_: 共通実装、Pi 拡張本体、Herdr runner

**処理整合性 (Operational integrity)**:
deadloopが、選んだ対象、検証した対象、書き込む対象を一致させ、並行更新、停止要求、未保存の変更を破壊せずに状態遷移する責任。exact head/base、検証記録、完了報告、Agent要求、認証主体、リポジトリidentityを外部書き込み時に結び付ける。リポジトリ所有者が定義したテスト、script、CI workflow、branch protectionの内容が十分かどうかは含まない。
_Avoid_: リポジトリ方針の品質監査、テスト強度の判定、検証script変更の特別扱い、branch protectionの複製、悪意ある同一ユーザーエージェントの封じ込め

**Automation host**:
deadloop core を定期または手動で呼び出し、試行回数と次回実行時刻を管理する実行主体。Issue / PR の選定、再試行可否、安全ゲートは判断せず、deadloop core の結果に従う。Pi 拡張、Claude App、Codex Appはそれぞれ別の Automation host になり得る。
_Avoid_: スケジューラ(定期実行だけを含意するため)、オーケストレータ(Pi上の具体的なhostだけを指すため)

**単発実行 (One-shot tick)**:
継続ループが無効なリポジトリで、通常ループと同じリポジトリ単位のlockと一時的な実行許可を取得し、通常のscheduler tickを正確に一回だけ実行する操作。候補選定、安全ゲート、push、コメント、ラベル遷移、自動マージを含む副作用と方針を変更せず、tick終了後は次のtickを予定せず無効状態へ戻る。一回の試行やIssueの完了を意味せず、実行中の試行や保留結果が残る場合がある。
_Avoid_: dry-run、一回の試行、Issueのend-to-end完了、単発実行だけ自動マージを無効にすること、通常ループと別のclaimやlock、次回tickの予約

**観測 (Observation)**:
計画を立てる直前に、GitHub、git、実行基盤、試行journalなどの外部状態を一度だけ読み取って固定した事実の値。計画はこの値だけから立てられ、workflowごとに取り直して前の実行の後の状態を映す。
_Avoid_: tick内で共有して頭の中で更新するスナップショット、計画中の追加読み取り、外部状態そのもの

**計画 (Plan)**:
一つの観測から立てられる、deadloopがこれから施す処置の並び。値であり、実行前に読め、実行係を差し替えれば実行せずに検討できる。計画を立てる処理は外部状態を読まず書かず、同じ観測には同じ計画を返す。
_Avoid_: 決定(設計決定と紛れるため)、アクション文字列、途中で観測しながら伸びる手順

**処置 (Step)**:
計画の一行。要求消費、起動、停止、マージ、人間への引き渡し、後片付けのように、この用語集の言葉で表せる一つの操作と、その前提を持つ。処置の中の細かい手順(実行基盤の操作、試行journalの記録、ラベル遷移の順番)は実行係が担い、計画には現れない。
_Avoid_: 手順(順番の含意が強いため)、コマンド、前の処置の結果を参照する行

**前提 (Precondition)**:
処置に添える、実行の直前にもう一度確かめる外部状態の条件。要求世代、head revision、ラベルの有無など。前提が成り立たなければその処置と以後の処置を実行せず、同じtick内で一度だけ観測と計画をやり直し、それでも成り立たなければ記録して次のtickに任せる。
_Avoid_: 計画時の確認だけで済ませること、無制限のやり直し

**実行係 (Executor)**:
計画の処置を順に、前提を確かめながら施す唯一の主体。deadloopが外部に施せる処置の種類は実行係の一覧に閉じ、workflowごとの別実装を持たない。計画と各処置の結果をhost-logに残す。
_Avoid_: workflow内の直接書き込み、複数の実行係

**受領書 (Receipt)**:
エージェント実行スクリプトが自分の仕事の完了を伝えるために置く、一度だけ書ける小さな記録。試行journalには書かず、Automation hostが次のtickで受領書を観測して試行journalを進める。
_Avoid_: 試行journalへの直接書き込み、完了報告との同一視

**計画確認 (Plan preview)**:
operatorが1コマンドで実行する、観測と計画だけを行い何も施さない読み取り専用の操作。計画をそのまま表示し記録する。lockも実行許可も取らず、単発実行とは別の操作である。
_Avoid_: dry-runモードの単発実行、doctor診断への混入、実行を伴う確認

**対応経路 (Support path)**:
Automation host、実行基盤、Agent programの検証対象となる組み合わせ。製品名単体ではなく、この組み合わせごとに第一級対応または実験的対応を判定する。
_Avoid_: 対応製品、全組み合わせの暗黙の保証

**第一級対応 (First-class support)**:
共通の受入契約を満たし、公開文書だけを使った作者以外の完走確認を終えた対応経路。利用者へ推奨できる。
_Avoid_: 実装済み、動作例あり、公式対応

**実験的対応 (Experimental support)**:
受入契約の一部が未達または未確認で、既知の制約と未確認能力を明示して試験する対応経路。利用者への推奨や自動マージの許可を含まない。
_Avoid_: 第一級対応、推奨経路

**オーケストレータ (Orchestrator)**:
deadloop 拡張を読み込んで動く常駐 Pi セッション。Automation host の一種としてautomationを定期実行し、Worker やレビューエージェントを起動・監視する。
_Avoid_: オーケストレーター(表記ゆれ。長音符なしに統一)、司令塔(旧称)、コーディネーター(automation の issue coordinator と混同するため)、ルーパー(プロダクト名 deadloop と衝突し、ループの主体は schedule のため)、メインセッション、親エージェント

**コード識別子 (Code identity)**:
Automation host が実行している deadloop 自身のコードを一意に定める識別子。deadloop checkout の commit で表し、ロード時に確定する。未コミットの変更は識別子にもコードスナップショットにも入らないため、開発中の編集は実行されず、停止の理由にもならない。
_Avoid_: revision(対象リポジトリの commit を指すため)、version(`package.json` の version と混同するため)、generation(disableGeneration と衝突するため)、鮮度(比較結果であって識別子ではないため)、ファイル更新時刻(戻した編集と進めた編集を区別できないため)

**コード更新の取り込み (Code reload)**:
Automation host が、配備中のコード識別子とロード済みの識別子の食い違いに気づいたとき、セッションが空いている tick 境界で自分の拡張を読み直し、スケジューラを再開する操作。同じ配備識別子には一回だけ試み、取り込めなければ停止して理由を表示する。走っている試行は殺さず、ホストの checkout も書き換えない。人が手で頼む入口も同じ。
_Avoid_: 自動リロード(起こり方であって操作の名前ではないため)、git pull(配備であって取り込みではないため)、プロセス再起動、進行中の試行の中断

**コードスナップショット (Code snapshot)**:
一つのコード識別子につき一度だけ作られる、読み取り専用の deadloop パッケージ複製。そのコード識別子の commit から作るため、未コミットの変更を含まない。エージェントが自分の session 内で実行するエージェント実行スクリプトの唯一の供給元であり、プロンプトに埋め込む絶対パスはここを指す。Automation host 自身はロード時の checkout のコードで動き、host が実行する workflow はその読み込み済みコードを in-process で呼ぶ。tick の開始が許される限り両者は同じコード識別子である。
_Avoid_: 試行ごとのコピー(識別子ごとに共有するため)、キャッシュ(性能目的ではないため)、working tree(可変でありエージェント向け供給元にしないため)、host が読み込む配備先(host は checkout から読み込むため)、`__dirname` から作るエージェント向けパス

**エージェント実行スクリプト (Agent-run script)**:
エージェントが自分の session から `node` で実行する deadloop のスクリプト。`run-project-check`、`pr-review-repair-finalize`、`pr-branch-update-finalize` の 3 本で、`extensions/deadloop/automations/` に置く薄い CLI の殻であり、実装は `src/` のモジュールを呼ぶ。host と agent の 2 つの利用者を持つ seam であり、Automation host が自分の処理をこの形で子プロセスとして起動することはない。
_Avoid_: driver、automation スクリプト(host が起動する意味を含むため)、`--fixture` による起動、host↔host の subprocess

**ワークフロー状態 (Workflow state)**:
GitHub上だけで読める、対象のIssueまたはPRの仕事の現在状態。Issue / PRの状態、head、ラベル、コメント、checksで表す。唯一の正本であり、ローカルの試行journalや実行基盤の観測結果はこれを複製しない。待機中の仕事はAgent要求ラベル、消費されて動いている仕事は`agent:in-progress`、安全に続行できない仕事は`agent:blocked`で表す。ローカルの資源だけを理由にGitHubへ見えない待機や状態を作ってはならない。
_Avoid_: journalとの二重管理、GitHubに存在しない作業中・停止状態、ラベル一覧とは別の内部状態による選定抑止

**Agent要求 (Agent request)**:
GitHub上で、対象のIssueまたはPRに対して特定の仕事を求めるWorkflow上の意思。`agent:implement`、`agent:review`、`agent:update-branch`などのラベル追加イベントで表す。IssueとPRで同じ意味を持ち、どの試行が仕事を所有するかは表さない。実行中に追加した要求は、現在の試行とは別の次の試行を求め、役割ごとに最大一つ待機する。停止中に役割に対応する新しいAgent要求を追加した場合も、新しい試行を求める。ローカルの試行記録はAgent要求ではなく、仕事を始める許可も与えない。
_Avoid_: claim、現在のラベル一覧だけ、ローカルの試行記録、`/deadloop-abandon-attempt`

**探索要求 (Explore request)**:
`agent:explore`ラベルの付与イベントとして一度だけ存在する、読み取り専用調査の依頼。実装要求より優先して処理され、消費されると読み取り専用の調査試行になる。調査結果は人間が読めるコメントとして保存し、待機中の実装要求は消さない。triage情報や実行条件ではなく、再利用できないイベントである。
_Avoid_: 実装の事前確認ステップ、`ready-for-agent`との組み合わせ条件、複数回使える依頼

**要求消費 (Request consumption)**:
一意に記録された試行が、起動前に一つのAgent要求を引き受け、待機中の要求から実行中の仕事へ移す遷移。Issueでは選んだ要求ラベルだけを外し、要求イベントidと削除後のタイムラインを照合してから`agent:in-progress`を追加する。PRでは`agent:in-progress`を非破壊的に追加し、開始時に存在した他の管理対象ラベルを個別に外した後、選んだAgent要求ラベルを最後に外す。どちらも選んだラベルの削除がHTTP 200で確認できた時点だけを消費の成立点とし、要求イベントidを試行記録へ残す。GitHubへ所有権コメントは書かず、試行記録も許可の根拠にはしない。一つのIssueの`agent:in-progress`は一つの消費だけに属し、その所有は、その追加イベントより前で直近の試行境界（作業状態の解除または停止）より後にある自動化の要求ラベル削除のうち、最も優先度の高い役割のもので決まる。調査は実装より優先されるため、実装の消費が同時の調査から作業状態を奪うことはない。認可された自動化identityのいずれかが書いた`agent:in-progress`だけが所有の対象になり、人が付けた作業状態は流用しない。identityが異なるホストが同じリポジトリを担当していても、同じタイムラインから同じ所有者を導く。所有しなかった消費は、自分が消したことを証明した要求ラベルだけを戻して次の試行の待機に残す。削除後に世代が変わっている場合は戻さず、説明だけを残す。所有しない消費は起動せず、耐久記録も残さず、動いている試行の作業状態に触れず、消えた自分の要求を説明だけ残す。新しい要求世代との競合を検知した場合は起動せず、現在の要求を後続の試行に残す。ラベルがない世代や状態が曖昧な世代は推測で戻さず、要求消費不明停止にする。破壊的な書き込みの安全性は対象revisionに対する再検証、試行の生存確認は実行基盤だけが担う。
_Avoid_: 所有権コメント、コメント時刻による排他制御、エージェント起動後の消費、試行を記録する前の消費

**要求消費不明停止 (Ambiguous request consumption blocked)**:
Agent要求の消費とその記録の間で停止し、要求をdeadloopが消費したのか人が取り消したのかを証明できないため、新しい試行を起動しない停止。要求を自動で復元せず、消費済みとも扱わない。GitHubに停止理由と、新しいAgent要求を追加する復旧手順を残す。
_Avoid_: 要求の自動復元、取消後の自動起動、説明のない要求消失

**停止コード (Stop code)**:
deadloopが人間に停止を伝えるときに発行する、次の行動を1つだけ示すコード。`add_request`、`free_storage`、`fix_environment`、`fix_verification_policy`、`wait` の5種であり、原因の詳細は停止コメントの本文が担う。監視判断、journal解放記録、doctor所見など、deadloop自身の次の処置を決める内部語彙は含まない。
_Avoid_: 診断コードの総覧、自由記述のreason、原因をコードに埋めること、複数の操作を示すコード

**試行 (attempt)**:
一つのIssue調査、Issue実装、またはPRレビューを一度遂行する単位。外部副作用より前に一意に識別され、エージェント起動前の復旧中は同じ試行として追跡される。起動後は、実行基盤が実行中と報告する場合だけ同じ試行の監視を続ける。起動した可能性を否定できない実行を同じ試行として重ねず、明確な失敗後の再実行は新しいAgent要求による新しい試行となる。完了報告は試行と対象revisionに結び付く。一つの試行は開始から完了まで一つのコード識別子で動き、途中でdeadloop側のコードが入れ替わることはない。
_Avoid_: run(定期実行やプロセス起動と混同するため)、session(エージェントの会話状態を指すため)、retry(試行間の関係だけを指すため)

**試行journal (Attempt journal)**:
一つの試行を起動準備から終了まで記録する、Automation host側の永続ファイル群。試行識別子、消費した要求イベントid、対象revision、launch設定、完了報告やreceiptの所在を持つ。監視、復旧、doctor診断の証拠であり、ワークフロー状態ではないため、GitHub上の要求の選定や抑止には使えない。
_Avoid_: ワークフロー状態の複製、エージェントの会話ログそのもの

**試行監視 (Attempt monitoring)**:
起動済みの試行について、実行基盤の生存状態と完了報告を決定論的に観測し、継続、完了、失敗、時間切れ、または不明を区別する処理。実行基盤が作業中と報告する間は出力の途切れだけで止めず、設定済みの総実行時間上限を超えた場合だけ時間切れとする。終了後に完了報告がなければ追加入力で催促せず、モデル利用待機または停止へ進む。モデルによる意味判断や会話を含まず、同じ観測結果には常に同じ判断を返す。
_Avoid_: 監視エージェント、監視プロンプト、軽量モデルによるポーリング、`nudge_worker`、完了報告の催促、出力時刻だけによる作業中エージェントの停止、実測前の短い時間上限、完了報告がない状態での無制限な待機

**容量不足停止 (Storage exhaustion blocked)**:
試行中の書き込みが `ENOSPC` または `EDQUOT` で失敗したため、その試行を安全に続行できない状態。事前に必要容量を予測する状態ではなく、完了報告または決定論的な処理結果で実際に観測した失敗を表す。対象の Issue または PR に理由を引き渡し、同じ試行を自動再試行しない。エージェントの画面出力だけでは容量不足停止と断定せず、完了報告なしの終了は原因不明の技術的失敗として区別する。
_Avoid_: 最低空き容量の予測、画面出力からの原因推測、完了報告なしの終了、一般的なエージェント失敗、警告だけで続行できる状態

**調査エージェント (Explorer)**:
Issue coordinator が Herdr worktree に起動する、単一Issueを読み取り専用で調査する使い捨てのエージェントセッション。リポジトリまたはGitHubを変更せず、構造化した調査結果を完了報告として返す。Automation host はHEADと未保存の作業を検証してから結果をIssueへ保存する。
_Avoid_: Worker、GitHubへ直接コメントするエージェント、調査結果を保存するAutomation host

**Worker (作業エージェント)**:
Issue coordinator が Herdr worktree に起動する、単一 issue を実装する使い捨てのエージェントセッション。どの CLI で動くかはエージェント種別が決める。
_Avoid_: 実装エージェント、子エージェント、Pi セッション(pi 固定を含意するため)

**レビューエージェント**:
PR reviewer が起動する、単一 PR をレビューする使い捨てのエージェントセッション。Worker とは別概念で、モデル指定も独立している。
_Avoid_: レビュワー(automation の PR reviewer と混同するため)、review worker

**外部レビューサービス (External review service)**:
GitHub PR に対してレビューを行う、deadloop の外部にあるサービス。deadloop のレビューエージェントとは区別する。標準では利用せず、利用者がセットアップ時に選んだ、型付きアダプターを持つサービスだけを自動で呼び出す。
_Avoid_: レビューエージェント、任意のシェルコマンドで呼び出すレビューBot

**レビュー結果 (Review outcome)**:
外部レビューサービスまたはdeadloopのレビューエージェントが、対象PRのheadに対して返す共通判定。確認中、レビュー合格、修正が必要、人間の判断が必要、確認不能を区別し、詳細な指摘本文とは分けて扱う。
_Avoid_: レビューコメント、マージ命令、headに結び付かない合否

**レビュー合格 (Approved review)**:
対象headに修正必須の指摘が一つも残っていないというレビュー結果。参考所見は残してよい。必須検証、CI、branch protection、自動マージの安全条件を通過したことは意味しない。
_Avoid_: 所見なし、CI成功、マージ可能、参考所見の禁止

**修正必須の指摘 (Required finding)**:
レビュー対象を先へ進める前に直す必要がある、根拠付きの指摘。`changes_requested`では一件以上が必要で、レビュー合格には一件も含められない。自動修復を行う場合は、この指摘だけが修復担当の契約になる。
_Avoid_: 参考所見、機械用指摘ID、コメント中の命令

**参考所見 (Advisory observation)**:
人間へ知らせる任意改善の所見。レビュー合格と同時に残せるが、自動修復の契約には渡さず、合否や修復可否を決めない。
_Avoid_: 修正必須の指摘、将来の修復予定、合格を妨げる所見

**修復進展 (Repair progress)**:
過去の修正必須の指摘が存在しないか、すべて解消済みであり、現在の修正必須の指摘がすべて新規であるとレビューエージェントがレビュー履歴から明示した状態。修復回数や変更ファイル数では判定しない。過去の指摘の継続、再発、または過去と新規の混在は修復進展ではなく、人間への引き渡しになる。
_Avoid_: 修復回数、変更量、本文fingerprintによる意味判定、deadloopによる自然言語の再判定

**レビュー履歴 (Review history)**:
一回のレビューが観測した、PRのコミット列、正確な差分、会話コメント、送信済みレビュー、行コメントの完全な時点情報。追加、編集、削除、head/baseまたは差分の変更でrevisionが変わり、古いレビュー結果は合格、修復、引き渡し、マージに使えない。コメント本文は人間向けの時系列証拠であり、安全制約を弱める命令や権限として扱わない。
_Avoid_: headだけのrevision、コメントを信頼済み命令として実行すること、投稿済みコメントの編集、指摘ID台帳

**作業中 (In progress)**:
Agent要求が消費されてから試行が終了または停止するまでの現在状態。`agent:in-progress`という一つのラベルで表し、役割や所有者を表さない。所有している試行は、認可された自動化identityが書いたこのラベルの追加イベントと、そこからのタイムライン照合で決定論的に導く。人が付けた作業状態は流用しない。`agent:reviewing`のような役割別の中間状態は廃止した。
_Avoid_: 役割別進捗ラベル、所有権コメント、同じ対象での複数試行の作業中、ローカルjournalだけによる作業中の維持

**人間への引き渡し (Human handoff)**:
deadloopが担当する確認を終え、取得できなかった証拠、修正要求、残る判断とともに次の操作を人間へ渡した状態。PRでは、draftをreadyへ変更しAgent系ワークフローラベルを1つも残さない状態で表す。Issueでは`ready-for-human`という分類ラベルで表す。どちらもマージ可能または人間承認済みであることは意味しない。
_Avoid_: マージ可能、承認済み、自動化の成功だけを表す状態、PRに付ける`ready-for-human`、停止

**停止 (Blocked)**:
deadloopが安全に続行できないと判断して仕事を止めた状態。`agent:blocked`で表し、待っているAgent要求を1つも持たない。「止めた」と「まだ求められている」は同時に成り立たないため、deadloopが判断して書く停止は、人が作業中に追加した要求ラベルも含めて要求を残さず、何を外したかと再開に何を足すかを説明のコメントで示す。停止と同居する要求ラベルは、必ず停止より後に人が追加したものになる。再開は役割に対応するAgent要求の追加であり、`agent:blocked`は次の試行が要求を消費した時点で消える。deadloopが担当を終えた人間への引き渡しとは区別する。
_Avoid_: 人間への引き渡し、要求を残したまま止めた状態、`agent:blocked`を人が外すことで再開する状態、最小の書き込みだけで止める経路が要求ラベルまで整えること

**実行基盤 (Execution runtime)**:
試行に結び付いたworkspaceとsessionの所有権を持ち、起動、観測、停止、安全な後片付けを提供する基盤。GitHubの状態や再試行可否は判断せず、未知またはdirtyな作業領域を保全する。Herdrは実行基盤の一種。
_Avoid_: runner(実装上のinterface名と混同するため)、Automation host(定期実行や試行管理の主体を指すため)、エージェント種別(Pi / Claude / Codexの差異を指すため)

**担当者不在 (Owner absent)**:
試行が名前で指すエージェントが、実行基盤の一覧に存在しない状態。存在の有無だけで決まり、状態値からは導かない。実行基盤だけが答える観測であり、GitHub上の停止とは別の事実。
_Avoid_: 停止(GitHubの状態を指すため)、入力待ち、完了、試行記録から推定する不在

**入力待ち (Awaiting input)**:
エージェントが実行基盤の一覧に存在し、ターンを終えて次の入力を待っている状態。完了したことも担当者不在も意味せず、完了の権威は完了報告だけ。
_Avoid_: 停止、担当者不在、完了報告の代わりに使うこと、ターン終了を仕事の終了と読むこと

**エージェント作業領域 (Agent scratch area)**:
deadloopが起動したエージェントCLIが、対象worktreeの中に自分の都合で作る未追跡のディレクトリ。対象リポジトリの成果物ではなく、operatorが管理する対象でもない。未保存の作業があるかを見る安全ゲートは、これを未保存の作業として数えない。追跡された時点でこの区分から外れ、通常の未保存の作業として扱う。
_Avoid_: 実行成果物(ADR 0010のdeadloop自身の出力を指すため)、エージェントCLIの設定ディレクトリ全体(共有される設定資産を含むため)、operatorの無視設定で吸収する対象

**エージェント種別 (workerAgent)**:
operator がプロジェクト設定で選ぶ、Worker を動かす CLI エージェントの種類(`pi` / `claude` / `omp` の列挙)。起動構文・prompt の渡し方・session 形式・promise 抽出方法が連動して決まる分岐キーであり、モデル指定とは独立。識別フラグを持たない CLI もある。未設定は `pi`。
_Avoid_: 起動コマンドテンプレート、workerCommand

**エージェントプロファイル (AgentProfile)**:
エージェント種別ごとの起動時差分(argv の形・prompt の渡し方・レベル写像・前提条件)を記述した、コード内の型付きテーブル。エージェント種別の列挙検証もここから導出される唯一の情報源。追加はプロトタイプ検証とテストを伴う PR で行い、operator 設定では拡張できない。
_Avoid_: 外部プロファイル設定、ユーザー定義エージェント

**ランチャー (launch-agent)**:
オーケストレータがエージェント(Worker / レビューエージェント)を起動するときに呼ぶ唯一のコマンド。エージェントプロファイルから argv を組み立て、シェルを介さずに実行基盤の起動コマンドを実行し、前提条件を fail-fast で検査する。
_Avoid_: プロンプトテンプレート内の起動コマンド分岐、argv 印字ヘルパー

**エージェント起動フロー (Agent launch flow)**:
オーケストレータが Worker / レビューエージェントを起動するために、実行基盤の worktree / tab を用意し、prompt / promise パスを作り、ランチャーを呼ぶ一連の手順。GitHub Issue / PR の選定やラベル状態遷移は含めない。
_Avoid_: GitHub Issue / PR の状態遷移、candidate selection、review gate

**役割別モデル指定 (Role model selection)**:
operator がプロジェクト設定で固定する、探索、Issue実装、PRレビュー、レビュー修復、ブランチ更新の各エージェントが使用するモデル。`workerModel`と`reviewerModel`は必須で、探索、修復、ブランチ更新のモデルは、個別指定がなければ`workerModel`を継承する。継承元もない場合は起動せず、エージェントCLIの既定モデルへは切り替えない。各役割の必要能力、副作用、利用枠に基づくoperatorの意思決定であり、オーケストレータの裁量ではない。
_Avoid_: 全役割共通の雑務モデル、エージェントCLIの既定モデル、実行時の自動モデル選択、Automation hostのモデル

**モデル使用記録 (Model usage record)**:
一つのモデル応答ごとに、試行、親子関係、役割、操作種別、エージェント種別、provider、model、入力・キャッシュ読み取り・出力・推論・合計トークン、所要時間、終了理由、エラー、料金見積りを結び付けた観測記録。親エージェント、追跡可能なサブエージェント、advisor、プラグイン経由のモデル呼び出しを含み、エージェント種別ごとのadapterがturn終了後かつworkspace終了前に、対象試行のcheckoutと証明できるセッションの記録だけを回収し、一意な応答・セッションidentityで重複を除いて状態領域へ保存する。関係を証明できないホスト全体のセッション履歴は取り込まず、保存量はその試行自身の応答数に比例する。同一の安定応答identityは集計全体で一度だけ数える。取得不能な項目は`unknown`、試行に結び付くが役割を証明できない応答は`unattributed`とし、回収失敗だけで完了済みの試行やGitHub状態遷移を失敗扱いにしない。プロンプト本文や応答本文は複製しない。応答単位の記録から試行、役割、provider、model、成否、再試行ごとの集計を導き、役割別モデル指定が意図した費用と利用枠に収まるかを実運用で評価する。試行の進行可否や完了を決める上限にはしない。
_Avoid_: workspace終了後だけの回収、worktree内だけに残る記録、親エージェントだけの集計、二重計上、試行をまたいだ同一応答の二重加算、関係を証明できないセッション履歴の試行記録への複製、試行合計だけの記録、実行時のモデル一致ゲート、トークン数による試行停止、取得不能な使用量を0と記録すること、役割を失った集計値、プロンプトや応答本文の複製、モデル使用記録を起動設定のテスト代わりにすること

**モデル利用待機 (Waiting for model availability)**:
エージェントのターンが終了して完了報告がなく、最終結果が既知の課金・利用権限拒否と一致したため、同じ試行、作業領域、エージェントセッションを保持してモデルの利用再開を待つ状態。最初の一回だけ人に見える形で理由を通知し、providerが再試行時刻を示した場合はそれに従い、示さない場合は通常の次回scheduler tickで、試行監視がモデルを使わず再実行する。モデル利用待機中は総実行時間上限の実作業時間を進めず、statusは実作業時間、待機時間、待機開始、次回時刻、回数を分けて示す。単発実行は次のtickを予定しないため待機時点で終了し、次の単発実行を人が開始する。作業中のセッションに現れる個別の一時エラーは判定材料にせず、エージェントCLI自身の回復に任せる。
_Avoid_: CI失敗、CI代替検証、GitHub Actionsの課金制限、セッション途中の`fetch failed`等からの判定、モデル利用待機を実作業時間に数えること、新しいworkspaceやworktreeを作る再試行、固定の再試行回数、provider指定を無視した独自間隔、高速な再試行、再試行ごとのGitHubコメント、トークン数だけによる判定

**必須検証 (Required verification)**:
Worker、修復担当、レビューエージェントが生成した対象revisionを受け入れる前に成功させる、対象リポジトリごとに解決済みの検証契約。自発的な追加検証は含まない。有効化時やエージェント起動前には実行せず、対象revisionでの実行結果をpush、人間への引き渡し、自動マージの安全ゲートにする。
_Avoid_: 有効化時の基準ブランチ検証、エージェント起動前の検証、検証の下限、推奨検証、エージェントが任意に選んだ検証

**必須検証記録 (Required verification record)**:
deadloop が必須検証を実行した事実と結果を、対象リポジトリ、commit、必須検証のコマンドと情報源、その導出に使った基準ブランチのrevisionに結び付けた構造化記録。成功、失敗、時間切れ、中断を区別し、成功記録だけを、すべての結び付きが一致する場合に別の役割から再利用できる。エージェントの自己申告は含まない。
_Avoid_: エージェントによる検証結果の文章、commitや検証契約に結び付かない実行結果

**基準検証 (Base diagnosis)**:
対象revisionの必須検証が失敗した場合だけ、同じ検証契約を試行が固定した信頼済みbase revisionで実行し、変更による失敗と既存のbase・検証環境・検証契約の問題を区別する診断。成功を許可する安全ゲートではなく、失敗理由を決めるために使う。
_Avoid_: 有効化時の事前検証、エージェント起動前の検証、対象revisionの必須検証を省略すること、baseの成功記録によるpushやマージの許可

**基準検証停止 (Base verification blocked)**:
基準検証が失敗したbase revisionと検証契約の組み合わせに対して、新しいエージェントを起動せず、GitHub上のAgent要求を未消費のまま保つ状態。`/deadloop-status`とdoctor診断に理由を示し、base revisionまたは検証契約が変わると自動的に解除する。
_Avoid_: IssueやPRごとに同じ壊れたbaseへエージェントを起動すること、Agent要求の消費、手動でしか解除できない停止

**CI相当検証契約 (CI-equivalent verification contract)**:
GitHub上の失敗したcheck全体を代替し、自動マージの正常性証拠として使用できる完全な検証契約。信頼済みbase revisionのリポジトリ方針に明示した一つのコマンドを優先し、未設定時は同じrevisionに`package-lock.json`と`package.json`の`scripts.check`があれば`npm ci && npm run check`を規約として解決する。それ以外は契約なしとしてCI代替検証を無効にし、明示した空コマンドは設定エラーとする。必須検証への追加分ではなく、依存準備を含めCI全体と同等になる一つのコマンドであり、deadloopは必須検証と自動合成しない。コマンド、導出方法、情報源、base revisionを記録する。マージ候補tree側のscriptや検証実装は、PR作成者とリポジトリ所有者が責任を持つ検証対象の一部としてそのまま実行する。
_Avoid_: 別の有効化フラグ、課金障害だけを選ぶmode、複数のlocal command設定、CI代替専用の自動マージフラグ、`npm ci`だけを検証とみなすこと、`scripts.check`がないnpm scriptの推測、npm以外のpackage managerや言語の推測、PR内の検証script変更を理由にした特別な自動マージ禁止、必須検証への追加コマンド、必須検証との自動合成、実行時にエージェントが選んだコマンド、情報源やbase revisionに結び付かない設定

**マージ候補tree (Prospective merge tree)**:
固定したPR headと現在のbaseを、実際のマージ方針と同じ意味で一時的に統合したtree。GitHubへ書き込まずに作り、head OID、base OID、tree OIDを一組の正常性確認対象として扱う。headまたはbaseが変わると同じ候補ではなくなる。
_Avoid_: PR head単体、base単体、headまたはbaseに結び付かない一時worktree、古い組み合わせの検証記録


**CI代替検証 (CI fallback verification)**:
GitHub上のcheckがすべて終了し、一つ以上が失敗していても、マージ候補treeに対するCI相当検証契約で同じ正常性を確認する検証。pending、queued、in-progressのcheckが一つでもあれば開始せず、checkが存在しないこと自体は失敗とみなさない。必須検証を常に含む。GitHub Actionsを正常性確認の唯一の権威とはせず、CI代替検証の成功は、人間への引き渡しと通常のGitHubマージ操作による自動マージの正常性証拠に使用できる。branch protectionやrulesetが通常のマージを拒否する場合は迂回せず停止する。成功記録はPR head、base、tree、検証契約に結び付き、自動マージ直前にheadまたはbaseが変わっていれば無効になる。成功してもCI成功とは呼ばない。
_Avoid_: CI成功、checkがない状態、pending checkとの並行実行、状態不明のcheck、CI相当検証契約がない状態、PR head単体での自動マージ許可、必須検証を省略するローカル検証、`--admin`やruleset bypass、branch protectionの複製実装

**CI代替修復 (CI fallback repair)**:
マージ候補treeのCI代替検証が失敗し、同じ契約によるbase診断は成功した場合に、その失敗ログを既存のレビュー修復経路へ渡してPR branchを直す、一回限りの自動修復。修復後にheadが変わっても同じ修復の流れとして追跡し、新しいマージ候補treeのCI代替検証が再び失敗したら停止する。人が新しいAgent要求を追加した場合だけ、新しい修復の流れを開始できる。
_Avoid_: head OIDごとに修復回数をリセットすること、失敗ログの文字列差分による無制限な修復、base診断も失敗した状態でのPR修復、二回目の自動修復

**検証候補 (Verification candidate)**:
CI定義やマニフェストから見つかったが、必須検証としてはまだ採用されていない検証。候補の発見だけでは完了条件にならない。
_Avoid_: 必須検証、実行すべき検証

**必須検証停止 (Required verification blocked)**:
必須検証の情報源がない、情報源が衝突している、または実行対象がないと決定論的に判明したため、書き込みを伴う試行を開始または完了できない状態。エージェントや変更内容の失敗ではなく、再試行回数を消費しない。必須検証を実行した結果の失敗とは区別する。
_Avoid_: 検証失敗、エージェント失敗、警告だけで続行できる状態

**完了報告 (Completion report)**:
Workerまたはレビューエージェントが試行の終了前に提出する、試行、対象revision、結果、要約、検証またはレビュー証拠を含む構造化報告。意味的な完了判定の唯一の権威だが、GitHub状態の変更やmergeを許可する命令ではない。エージェントの停止を確認した後にもう一度読み、存在しなければ待機を続けず、完了報告なしの技術的失敗として扱う。
_Avoid_: process終了、画面出力、session状態、エージェントの最終回答、停止後に存在しない報告を待ち続けること

**promise**:
Pi + Herdr経路で、起動ごとの専用パスを使って完了報告を搬送する仕組み。完了報告そのものの形式やdeadloop coreの公開契約とは区別する。
_Avoid_: 完了報告との同一視、promiseテキスト規約(`<promise>`タグ)、pane grep、session JSONL抽出

**起動ポリシー (workerLaunchPolicy)**:
issue の難易度から `low` / `medium` / `high` のレベルを選ぶための、オーケストレータ向けの方針文。モデル選択やエージェント固有フラグ名はこのポリシーの管轄外。
_Avoid_: launch policy でのモデル許可、`--thinking` / `--effort` 固定の方針文

**doctor 診断**:
operator が 1 コマンドで実行する、既知の失敗モードの読み取り専用診断。所見ごとにコピペ可能な確認コマンドまたは解決コマンドを提示するが、自動修復はしない。停止した仕事の再実行には、ローカルの復旧コマンドではなく、役割に対応する新しいAgent要求を追加するGitHubコマンドを提示する。
_Avoid_: 自動修復、試行を再開または放棄するdeadloopコマンド、他セッションが実行中のコードの断定(共有状態に残った足跡からの推定に留めるため)
